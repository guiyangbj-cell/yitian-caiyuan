# 一天菜园 v0.1 前台原型

这是「一天菜园」第一版前台壳：今天、地图、生长、四季、拍一下。当前使用假数据，不接数据库、不接 AI、不接天气。

## 本地运行

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

Netlify 部署设置：

- Build command: `npm run build`
- Publish directory: `dist`
